#
# ~/.bashrc
#   haciendo cambios para probar chezmoi

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
PS1='[\u@\h \W]\$ '
export EDITOR=code
export VISUAL=code
export LESS=-R